# Проект ОПП
## Описание
Использование классов и объектов на основе популярной темы e-commerce.
E-commerce — электронная торговля, или электронная коммерция. 
На данном этапе работы мы не будем реализовывать систему платежей, 
однако подготовим всё для того, чтобы у нас появилось ядро для интернет-магазина. 
В дальнейшем для этого ядра возможно будет реализовать любой интерфейс — 
от сайта до телеграм-бота.
___
## Активация пространства
poetry init
poetry add pytest
___
## Модули
- category
- product
- read_json - с функцией чтения json-файла с категориями
___
## Классы
- class BaseCategory - Абстрактный класс с перечислением методов для класса Category и его дочерних классов.
- class BaseProduct - Абстрактный класс указывает на то, что абстрактные методы должны реализоваться во всех
    наследованных классах.
- class MixinPrint - Миксин класс который при инициализации показывает в консоль имя класса и его аттрибуты.
- class Category (наследник BaseCategory)
  с атрибутами name: str,
      description: str,
      products: list,
      category_count = 0,
      product_count = 0.
- class Product (наследник BaseProduct и MixinPrint) 
    с атрибутами 
    name: str,
    description: str,
    price: float,
    quantity: int.
- class Iterator - для итерации объектов
- class Smartphone(наследник Product)
    efficiency: float,
    model: str,
    memory: int,
    color: str.
- class LawnGrass(наследник Product)
    country: str,
    germination_period: str,
    color: str.
- class Order(BaseCategory):
    Класс "Заказ", который наследуется от абстрактного класса и получает общие абстрактные методы
___
## Описание class Category
### Методы
- init - Метод для инициализации экземпляра класса. Задаем значения атрибутам экземпляра.
- add_product - Метод для добавления продукта в категорию.
- str - магический метод для строкового вывода экземпляра класса.
- middle_price - метод, который подсчитывает средний ценник всех товаров. 
С помощью исключений обработать случай, когда в категории нет товаров и сумма всех товаров 
будет делиться на ноль. Если такое происходит, возвращайте ноль.
### Геттеры, сеттеры, делитеры
- products - Getter, который возвращает строку, информацию по продуктам.
- prod_in_list - Getter, который возвращает словарь, информацию по продуктам
- prod_in_list - Сеттер, который возвращает словарь, информацию по продуктам
___
## Описание class Product
### Методы
- init - Метод для инициализации экземпляра класса. Задаем значения атрибутам экземпляра.
- new_product - Класс-Метод для добавления продукта в класс Product.
- str - магический метод для строкового вывода экземпляра класса.
- add - магический метод для сложения общей цены для продуктов (цена*кол-во).
### Геттеры, сеттеры, делитеры
- price - Getter, который возвращает информацию по цене.
- price - Setter, который проверяет цену на положительность, возвращает вывод с ошибкой.
___
## Описание class Iterator
### Методы
- iter - Метод для получения итератора для перебора объекта.
- next - Метод для перехода к следующему значению и его считывание.
___
## Описание class Smartphone
### Методы
- init - расширен метод для инициализации экземпляра класса. Задаем значения атрибутам экземпляра.
- add - переопределен магический метод для сложения общей цены для продуктов (цена*кол-во).
___
## Описание class LawnGrass
### Методы
- init - Метод для инициализации экземпляра класса. Задаем значения атрибутам экземпляра.
- add - переопределен магический метод для сложения общей цены для продуктов (цена*кол-во).
___
## Описание class Order
### Методы
- init - расширен метод для инициализации экземпляра класса.
- add_product - Абстрактный метод для добавления нового продукта в заказ
- get_total_cost - Абстрактный метод для получения общей стоимости заказа
___
## Тесты
Проект включает набор тестов для проверки корректности работы.
### Запуск тестов
Для запуска тестов установите pytest:
```
pip install -U pytest
pip install pytest-cov
```
### Структура тестов
Тесты расположены в директории `tests/` и включают проверки для:
* основных функций
* граничных случаев
* обработки ошибок
### Требования
Для запуска тестов необходимы:
```
pytest
pytest-cov
```
### Покрытие тестами
Для проверки покрытия используйте:
```
pytest --cov
```
Для создания html-отчета используйте:
```
pytest --cov=src --cov-report=html
```
___
## Проверка PEP8
Для запуска проверки установите:
```
pip install flake8
pip install black
pip install isort
pip install mypy
```
## Документация:
Для получения дополнительной информации обратитесь к [документации](README.md).
## Лицензия:
Этот проект лицензирован по [лицензии MIT](LICENSE).